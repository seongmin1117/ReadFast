version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: rf-mysql
    restart: always
    ports:
      - "3310:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: read-fast
    volumes:
      - rf-mysql-data:/var/lib/mysql

volumes:
  rf-mysql-data:
